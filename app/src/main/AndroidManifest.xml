<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kursrpmos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Statistics_income"></activity>
        <activity android:name=".Statistics" />
        <activity android:name=".Dialog_incomes" />
        <activity android:name=".Dialog_costs" />
        <activity android:name=".Dialog" />
        <activity android:name=".Income" />
        <activity android:name=".Costs" />
        <activity android:name=".Choose_cost" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>